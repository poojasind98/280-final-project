NEXT_PUBLIC_API_BASE_URL=https://prolog-api.profy.dev

# Optional — enables the live OpenAI provider for the AI Copilot features.
# If unset, the app falls back to a built-in deterministic mock provider so all
# AI flows remain demonstrable without spend. Never expose this key to the
# browser; it is read only on the server.
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini
# OPENAI_BASE_URL=https://api.openai.com/v1
